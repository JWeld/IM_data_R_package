% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{im_detection_limit}
\alias{im_detection_limit}
\title{Drop or mark values below the detection limit}
\usage{
im_detection_limit(
  x,
  action = c("half", "drop", "na", "keep"),
  estimated = FALSE
)
}
\arguments{
\item{x}{A tibble from \code{\link[=im_read]{im_read()}}.}

\item{action}{What to do with below-detection values: \code{"half"} replaces them
with half the reported detection limit (the usual convention),
\code{"drop"} removes the rows, \code{"na"} sets \code{VALUE} to \code{NA}, and \code{"keep"}
leaves them but is useful together with \code{estimated}.}

\item{estimated}{Logical. Also drop values flagged as estimated (\code{"E"})?}
}
\value{
A tibble.
}
\description{
Values below detection are published as the detection limit itself with
\code{FLAGQUA == "L"}. Treating them as measurements biases means upward; simply
dropping them biases downward. This makes the choice explicit.
}
\examples{
pc <- im_read_file(im_example("sample_PC.csv"))
im_detection_limit(pc, action = "half")
}
