% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{im_latest_version}
\alias{im_latest_version}
\alias{im_version_exists}
\title{Which dataset versions exist}
\usage{
im_latest_version()

im_version_exists(version)
}
\arguments{
\item{version}{Version to check, as a string.}
}
\value{
\code{im_latest_version()} returns a string, or \code{NA} if the repository
could not be reached. \code{im_version_exists()} returns a single logical.
}
\description{
\code{im_latest_version()} asks the repository what the newest published version
is. \code{im_version_exists()} checks a particular one.
}
\details{
These are the basis of \code{\link[=im_check_version]{im_check_version()}}, which is the one you would
normally call. All of them need a network connection and return \code{NA} (or
\code{FALSE}) without one.
}
\examples{
\donttest{
if (curl::has_internet()) {
  im_latest_version()
}
}
}
