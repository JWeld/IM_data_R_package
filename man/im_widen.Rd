% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{im_widen}
\alias{im_widen}
\title{Pivot to one column per substance or parameter}
\usage{
im_widen(
  x,
  names_from = NULL,
  values_from = "VALUE",
  id_cols = NULL,
  values_fn = NULL
)
}
\arguments{
\item{x}{A tibble from \code{\link[=im_read]{im_read()}}.}

\item{names_from}{Column supplying the new column names. Defaults to
\code{SUBST} or \code{PARAM}, whichever the subprogramme uses.}

\item{values_from}{Column supplying the values. Defaults to \code{VALUE}.}

\item{id_cols}{Optional character vector of columns to keep as identifiers.
Defaults to the sample-identifying columns present in \code{x}.}

\item{values_fn}{Optional function to combine duplicate values, e.g. \code{mean}.
Without it, duplicates are an error.}
}
\value{
A wider tibble.
}
\description{
The published data are long: one row per measured value. Most analyses want
one row per sample and one column per determinand. This pivots safely.
}
\details{
The identifying columns default to everything that distinguishes a sample -
including \code{FLAGSTA}, which matters: in \code{AM} the same site, level and month
carries a mean, a minimum and a maximum as separate rows, and dropping the
flag from the key silently collapses them. If a key still has duplicates,
this errors rather than quietly taking the first value; \code{values_fn} gives
you a way to resolve them deliberately.

Units are dropped from the value columns because they vary between rows.
Check them first with \code{\link[=im_units]{im_units()}} and convert if needed.
}
\examples{
pc <- im_read_file(im_example("sample_PC.csv")) |> im_decode()
im_widen(pc)
}
