% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decode.R
\name{im_decode}
\alias{im_decode}
\title{Add readable names for the code columns}
\usage{
im_decode(x, quiet = TRUE, version = im_version())
}
\arguments{
\item{x}{A data frame from \code{\link[=im_read]{im_read()}} or \code{\link[=im_read_file]{im_read_file()}}.}

\item{quiet}{Logical. Suppress notes about codes that could not be matched.}

\item{version}{Dataset version the code lists should come from. Defaults to
\code{\link[=im_version]{im_version()}}. Lists published with a version this package was not built
against are fetched and cached automatically, so a release that adds
substances decodes without any change here.}
}
\value{
\code{x} with added lowercase columns: \code{substance} or \code{parameter},
and where the relevant column exists, \code{determination}, \code{pretreatment},
\code{stat} and \code{quality}.
}
\description{
Joins the published code lists onto a table so that \code{SUBST}, \code{PARAM},
\code{DETER}, \code{PRETRE}, \code{FLAGSTA} and \code{FLAGQUA} gain readable companion columns.
\code{\link[=im_read]{im_read()}} calls this by default; use it directly if you read a file
yourself or passed \code{decode = FALSE}.
}
\details{
Codes in the published lookup files are space-padded to a fixed width
(\code{"NA      "}, \code{"AOT40   "}) while the data files are not, so a naive join
matches almost nothing. The bundled lookups are trimmed, which is why this
works.
}
\section{Two code lists, not one}{
The determinand column draws on two published lists, and a companion column
says which: \code{LISTSUB} for \code{SUBST}, \code{PARLIST} for \code{PARAM}. \code{"DB"} means the
substance list (\link{im_substances}) and \code{"IM"} means the parameter list
(\link{im_parameters}). Roughly 26,000 rows across seven subprogrammes use \code{IM}
codes - \code{AOT40}, \code{SOL_G}, \code{CEC_E}, \code{C/N}, \code{BDEN} - and looking those up in
the substance list alone returns nothing.

It also matters where the lists overlap. 77 codes appear in both, and \code{ABS}
means \emph{Absorbance} in one and \emph{Number of branches on the current tree where
algae are missing} in the other. Both readings occur in \code{AL}, in the same
file, told apart only by \code{PARLIST}.
}

\examples{
\donttest{
if (curl::has_internet()) {
  raw <- im_read("MC", decode = FALSE)
  im_decode(raw)
}
}
}
