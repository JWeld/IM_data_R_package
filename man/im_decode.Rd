% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decode.R
\name{im_decode}
\alias{im_decode}
\title{Add readable names for the code columns}
\usage{
im_decode(x, quiet = TRUE)
}
\arguments{
\item{x}{A data frame from \code{\link[=im_read]{im_read()}} or \code{\link[=im_read_file]{im_read_file()}}.}

\item{quiet}{Logical. Suppress notes about codes that could not be matched.}
}
\value{
\code{x} with added lowercase columns: \code{substance} or \code{parameter},
and where the relevant column exists, \code{determination}, \code{pretreatment},
\code{stat} and \code{quality}.
}
\description{
Joins the published code lists onto a table so that \code{SUBST}, \code{PARAM},
\code{DETER}, \code{PRETRE}, \code{FLAGSTA} and \code{FLAGQUA} gain readable companion columns.
\code{\link[=im_read]{im_read()}} calls this by default; use it directly if you read a file
yourself or passed \code{decode = FALSE}.
}
\details{
Codes in the published lookup files are space-padded to a fixed width
(\code{"NA      "}, \code{"AOT40   "}) while the data files are not, so a naive join
matches almost nothing. The bundled lookups are trimmed, which is why this
works.
}
\examples{
\donttest{
if (curl::has_internet()) {
  raw <- im_read("MC", decode = FALSE)
  im_decode(raw)
}
}
}
