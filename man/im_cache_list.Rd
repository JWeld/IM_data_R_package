% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{im_cache_list}
\alias{im_cache_list}
\alias{im_cache_clear}
\title{List and clear cached files}
\usage{
im_cache_list(version = im_version())

im_cache_clear(version = im_version(), confirm = interactive())
}
\arguments{
\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}

\item{confirm}{Logical. If \code{TRUE} (the default in an interactive session),
ask before deleting. Set \code{FALSE} to clear without prompting.}
}
\value{
\code{im_cache_list()} returns a tibble with one row per cached file and
columns \code{file}, \code{size_mb} and \code{modified}; it has zero rows if nothing is
cached. \code{im_cache_clear()} returns, invisibly, the paths it removed.
}
\description{
List and clear cached files
}
\examples{
im_cache_list()
}
