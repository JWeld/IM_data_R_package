% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{im_provenance}
\alias{im_provenance}
\title{What this data came from}
\usage{
im_provenance(x)
}
\arguments{
\item{x}{An object from \code{\link[=im_read]{im_read()}}.}
}
\value{
An \code{icpim_provenance} object: a list with \code{subprog}, \code{file},
\code{dataset_version}, \code{doi}, \code{downloaded}, \code{read_at}, \code{package_version} and
\code{rows_read}. Returns \code{NULL}, with a warning, if \code{x} carries no record.
}
\description{
Reports the dataset version, DOI and file behind an object returned by
\code{\link[=im_read]{im_read()}}, together with when it was downloaded and which version of this
package read it.
}
\details{
The version of \code{icpim} does not record which data you used, and cannot:
\code{icpim.version} is a runtime option, so the same package version reads any
release. The record is attached to the data instead, which is what makes it
answer the question "what did this analysis actually use".
}
\section{What preserves it}{
The record survives \code{[}, \code{\link[dplyr:filter]{dplyr::filter()}}, \code{\link[dplyr:mutate]{dplyr::mutate()}},
\code{\link[dplyr:select]{dplyr::select()}}, \code{\link[dplyr:arrange]{dplyr::arrange()}}, \code{head()} and \code{saveRDS()}, so it is
still there after the usual reshaping and after a saved object is reloaded.
\code{\link[=im_widen]{im_widen()}}, \code{\link[=im_decode]{im_decode()}} and \code{\link[=im_detection_limit]{im_detection_limit()}} carry it forward
explicitly.

It is dropped by operations that build a genuinely new table, notably
\code{\link[dplyr:summarise]{dplyr::summarise()}} and joins. Capture it before aggregating if you need
it afterwards.
}

\examples{
pc <- im_read_file(im_example("sample_PC.csv"))
im_provenance(pc)
}
