% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_codes.R
\name{im_update_codes}
\alias{im_update_codes}
\title{Fetch the code lists published with a given version}
\usage{
im_update_codes(version = im_version(), quiet = NULL)
}
\arguments{
\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}

\item{quiet}{Logical. Suppress progress messages.}
}
\value{
Invisibly, the path of the cached code lists, or \code{NULL} if they
could not be fetched.
}
\description{
The lookups bundled with this package come from version 1. A later release
can add substances, parameters, methods or sites, so for any other version
the published lists are downloaded and cached, then used in preference to
the bundled ones.
}
\details{
This happens on its own the first time you read a version the package was
not built against, so you do not normally need to call it. Use it to
refresh a cached copy, or to warm the cache before going offline.
}
\examples{
\donttest{
if (curl::has_internet()) {
  im_update_codes()
}
}
}
