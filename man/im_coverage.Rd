% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{im_coverage}
\alias{im_coverage}
\title{Measure what a release actually contains}
\usage{
im_coverage(subprog = "all", version = im_version(), quiet = NULL)
}
\arguments{
\item{subprog}{Subprogramme code(s), or \code{"all"}.}

\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}

\item{quiet}{Logical. Suppress progress messages.}
}
\value{
A tibble with one row per subprogramme: \code{subprog}, \code{name},
\code{n_rows}, \code{n_sites}, \code{first_year}, \code{last_year}, \code{n_determinands} and
\code{size_mb}.
}
\description{
Row counts, site counts and year ranges are properties of a particular
release, so they are measured from the data rather than recorded in this
package. That means they stay right when a new version is published.
}
\details{
Files are downloaded and cached as needed, so the first call over \code{"all"}
fetches the whole deposit (about 95 MB). Later calls read from the cache.
}
\examples{
\donttest{
if (curl::has_internet()) {
  # One small subprogramme
  im_coverage("MC")
}
}
}
\seealso{
\code{\link[=im_manifest]{im_manifest()}} for the file list without downloading anything.
}
