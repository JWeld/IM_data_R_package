% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example.R
\name{im_example}
\alias{im_example}
\title{Small extracts of the real data, bundled for offline use}
\usage{
im_example(file = NULL)
}
\arguments{
\item{file}{Name of the example file. If \code{NULL}, the available names are
returned.}
}
\value{
A file path, or a character vector of available names.
}
\description{
Three genuine extracts from version 1 of the deposit, small enough to ship
with the package. They exist so that examples, tests and the vignette run
without a network connection, and because each one carries a trap worth
demonstrating:
}
\details{
\itemize{
\item \code{sample_PC.csv} — precipitation chemistry, site SE14, 2015–2019. Contains
60 rows whose substance code is blank because it is sodium, and a station
code of \code{"0176"} that must keep its leading zero.
\item \code{sample_AM.csv} — meteorology, site AT01, 2002. Air and soil temperature
and precipitation, carrying all of \code{X}, \code{A}, \code{Z}, \code{XA}, \code{XZ}, \code{S} and \code{SZ},
so the same key appears five times over.
\item \code{sample_BB.csv} — the complete bird subprogramme, 71 rows, every one of
them an annual value published with month \code{00}.
}
}
\examples{
im_example()
pc <- im_read_file(im_example("sample_PC.csv"))
# Sodium restored, and the station code kept its leading zero:
unique(pc$SCODE)
sum(pc$SUBST == "NA", na.rm = TRUE)
}
