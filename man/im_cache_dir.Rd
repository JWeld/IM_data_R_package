% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{im_cache_dir}
\alias{im_cache_dir}
\title{Where \code{icpim} stores downloaded files}
\usage{
im_cache_dir(version = im_version(), create = FALSE)
}
\arguments{
\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}

\item{create}{Logical. Create the directory if it does not exist?}
}
\value{
The cache path, as a character vector of length one.
}
\description{
The published files total roughly 95 MB, so they are downloaded once and
cached rather than shipped with the package. The cache lives under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}} and is keyed by dataset version, so pinning a
different version with \code{options(icpim.version=)} never mixes files from
two releases.
}
\details{
Set \code{options(icpim.cache_dir = "/some/path")} to override, or the
environment variable \code{ICPIM_CACHE_DIR}. Using a project-local directory is
a good way to make an analysis self-contained.
}
\examples{
# Where files would be stored (does not create anything):
im_cache_dir(create = FALSE)
}
