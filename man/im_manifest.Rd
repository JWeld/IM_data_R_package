% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{im_manifest}
\alias{im_manifest}
\title{The files published in a given version}
\usage{
im_manifest(version = im_version(), type = c("data", "documentation", "all"))
}
\arguments{
\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}

\item{type}{\code{"data"} for the subprogramme files, \code{"documentation"} for the
manual and code lists, or \code{"all"}.}
}
\value{
A tibble with \code{subprog}, \code{name}, \code{file}, \code{type} and \code{size_mb}.
The \code{subprog} and \code{name} columns are \code{NA} for documentation files.
}
\description{
Reads the file list straight from the repository, so a release that adds,
renames or resizes a file is picked up without this package being changed.
Falls back to the bundled catalogue when offline.
}
\examples{
\donttest{
if (curl::has_internet()) {
  im_manifest()
}
}
}
