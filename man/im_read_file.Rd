% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{im_read_file}
\alias{im_read_file}
\title{Read one published CSV file from disk}
\usage{
im_read_file(path, repair = TRUE, quiet = TRUE)
}
\arguments{
\item{path}{Path to a published ICP IM CSV file.}

\item{repair}{Logical. Restore the blank sodium substance code? See
\code{\link[=im_read]{im_read()}}.}

\item{quiet}{Logical. Suppress the note about repaired rows.}
}
\value{
A tibble, with \code{date}, \code{year} and \code{month} added.
}
\description{
The reading and typing half of \code{\link[=im_read]{im_read()}}, without the download, cache or
filtering. Use it for a file you fetched by hand, or for the bundled
examples in \code{\link[=im_example]{im_example()}}.
}
\details{
It does not decode the code lists; pass the result to \code{\link[=im_decode]{im_decode()}} if you
want readable names.
}
\examples{
pc <- im_read_file(im_example("sample_PC.csv"))
str(pc[, c("AREA", "SCODE", "SUBST", "VALUE", "date")])
}
