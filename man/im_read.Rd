% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{im_read}
\alias{im_read}
\title{Read a subprogramme}
\usage{
im_read(
  subprog,
  sites = NULL,
  countries = NULL,
  years = NULL,
  substances = NULL,
  decode = TRUE,
  repair = "auto",
  quiet = NULL,
  version = im_version()
)
}
\arguments{
\item{subprog}{Two-letter subprogramme code, e.g. \code{"PC"} for precipitation
chemistry. See \link{im_subprogrammes} for the full list.}

\item{sites}{Optional character vector of site codes (the \code{AREA} column),
e.g. \code{c("SE14", "SE15")}. Case-insensitive.}

\item{countries}{Optional character vector of countries, given either as
the two-letter ISO code that prefixes \code{AREA} (\code{"SE"}) or as the full name
as it appears in \code{COUNTRY} (\code{"Sweden"}). Case-insensitive.}

\item{years}{Optional numeric vector of years to keep, e.g. \code{2000:2019}.}

\item{substances}{Optional character vector of substance or parameter codes
to keep, matched against \code{SUBST} or \code{PARAM} as appropriate, e.g.
\code{c("SO4S", "NO3N")}. Use \code{"NA"} for sodium.}

\item{decode}{Logical. Join the code lists to add readable name columns?}

\item{repair}{Correct the blank sodium substance code. \code{"auto"}, the
default, applies the correction to version 1 and leaves every later
version untouched, since the issue is fixed from version 2 onwards. A
correctly coded file needs no repair and is passed through unchanged
whatever this is set to. \code{TRUE} forces the correction, \code{FALSE} returns
the file exactly as published. Under \code{"auto"}, blank codes found in a
version that should not have them are reported rather than assumed to be
sodium.}

\item{quiet}{Logical. Suppress progress and one-time notes. It does not
silence the warning raised when a filter matches nothing, which is a
diagnostic rather than progress; use \code{\link[=suppressWarnings]{suppressWarnings()}} for that.}

\item{version}{Dataset version. Defaults to \code{\link[=im_version]{im_version()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per published observation.
A filter that matches nothing returns an empty table and warns, rather
than leaving you to work out which argument was wrong.
}
\description{
Downloads (if needed), caches, reads and tidies one subprogramme of the ICP
IM open dataset. This is the main entry point of the package.
}
\section{What this does beyond reading the CSV}{
\itemize{
\item \strong{Types every column explicitly.} \code{SCODE} keeps its leading zeros;
\code{VALUE} is numeric; nothing is guessed.
\item \strong{Corrects the sodium code on read.} A known issue affecting \strong{version 1
only}: the substance code for sodium is blank in 48,444 rows across
thirteen subprogrammes, the literal string \code{"NA"} having been consumed as a
missing value before publication. It is corrected in version 2 onwards.
By default this is put right on read for version 1 and left alone
afterwards; see the \code{repair} argument.
\item \strong{Decodes the code lists.} \code{SUBST}/\code{PARAM}, \code{DETER} and \code{PRETRE} gain
readable companion columns (\code{substance}, \code{determination}, \code{pretreatment}).
\item \strong{Parses dates.} \code{YYYYMM} becomes \code{date}, \code{year} and \code{month}. Annual
values are published with month \code{00}; these get \code{month = NA} and a \code{date}
at 1 January, rather than failing to parse.
\item \strong{Decodes the flags.} \code{FLAGSTA} becomes \code{stat} and \code{FLAGQUA} becomes
\code{quality}. Read the note on \code{FLAGSTA} below before aggregating anything.
}
}

\section{Column naming}{
Columns in UPPERCASE are exactly as published, so they match the ICP IM
Manual and the dataset README one for one. Columns in lowercase are added
by this package.
}

\section{\code{FLAGSTA} is part of the key, not an annotation}{
In \code{AM} (meteorology) especially, the same site, level, month and parameter
carries several rows distinguished only by \code{FLAGSTA}: the monthly mean
(\code{X}), minimum (\code{A}), maximum (\code{Z}), and the averaged daily extremes
(\code{XA}, \code{XZ}). 47\% of air- and soil-temperature keys have more than one.
Averaging without filtering mixes means with extremes; for \code{PREC} it mixes
monthly sums (\code{S}) with maximum daily sums (\code{SZ}) and monthly maxima (\code{Z}).
Select on the \code{stat} column, or on \code{FLAGSTA} itself, before aggregating.
}

\examples{
\donttest{
if (curl::has_internet()) {
  # A temporary cache, so the example leaves nothing behind. In normal use
  # leave the default, which persists between sessions.
  op <- options(icpim.cache_dir = tempfile())

  # try(): the repository may be unreachable even when the network is up.
  try({
    # Metal chemistry in mosses is the smallest subprogramme, ~14 kB:
    mc <- im_read("MC", countries = "EE")
    print(head(mc))

    # Sodium: the code really is the string "NA".
    print(unique(mc$substance[mc$SUBST == "NA"]))
  })
  options(op)
}
}
}
\seealso{
\code{\link[=im_widen]{im_widen()}} to pivot to one column per substance,
\link{im_subprogrammes} for what is available. \code{\link[=im_download]{im_download()}} only fills the
cache and returns paths; you do not need it before calling this.
}
