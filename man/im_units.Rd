% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{im_units}
\alias{im_units}
\title{Report the units used for each determinand}
\usage{
im_units(x)
}
\arguments{
\item{x}{A tibble from \code{\link[=im_read]{im_read()}}.}
}
\value{
A tibble with one row per determinand and unit, with counts. A
determinand appearing more than once uses more than one unit.
}
\description{
Units are published per row and are not always consistent within a
subprogramme: foliage chemistry, for instance, reports the same elements in
both \code{mg/kg} and \code{ug/g}. Check this before pivoting or aggregating.
}
\examples{
\donttest{
if (curl::has_internet()) {
  im_units(im_read("FC"))
}
}
}
